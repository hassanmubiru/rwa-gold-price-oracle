# Secrets for the RWA Gold Price Oracle workflow.
# No secrets are required for this workflow since the CoinGecko free API
# does not require an API key and we use a public Sepolia RPC.
# Add secrets here if you expand to use an authenticated data provider.
secretsNames: {}
